version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../../
            - nvm i
            - nvm use
            - npm install
        build:
          commands:
            - npx nx build web
            - cd apps/web
        postBuild:
          commands:
            - ls -la
      artifacts:
        baseDirectory: ../../dist/apps/web/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - apps/web/.next/cache/**/*
