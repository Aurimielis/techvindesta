version: 1
applications:
  - frontend:
      phases:
        build:
          commands:
            - npm install
            - npx nx build web
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: apps/web
